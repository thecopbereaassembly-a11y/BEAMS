/**
 * Supabase-generated database types.
 *
 * PLACEHOLDER — regenerate from the live schema once migrations are applied:
 *   npm run db:types   (runs: supabase gen types typescript --local)
 *
 * Until then this keeps imports type-safe without hand-maintaining shapes.
 */
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
    CompositeTypes: Record<string, never>;
  };
};
